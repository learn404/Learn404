---
title : Le Terminal de commande
slug : introduction-au-terminal
publishedAt: "2024-03-01"
link: ["https://www.youtube.com/watch?v=jNqYK2k-zx4", "https://www.github.com/julien-maurel/terminal-commands"]

---

Apprenez à utiliser le terminal, un outil puissant qui vous permet de taper des commandes pour effectuer diverses tâches sur votre ordinateur. Allons-y !

---

> ❓ **Pourquoi en parler ?**
>
> Apprendre à utiliser le terminal est essentiel pour devenir développeur. Il permet d'être plus rapide et efficace. Dans notre cas, il sera utile pour installer des packages pour nos projets et surtout… pour les démarrer !

---

## Pourquoi utiliser le Terminal ?

Le terminal est un outil incontournable pour les développeurs et les utilisateurs avancés. Voyons pourquoi :

### Gain de Productivité

Utiliser le terminal peut grandement accélérer votre flux de travail. Plutôt que de naviguer dans des menus graphiques, vous pouvez exécuter des commandes directement. Par exemple, créer un dossier, déplacer des fichiers ou lancer des scripts se fait en quelques frappes au clavier.

### Automatisation des Tâches

Avec le terminal, vous pouvez automatiser des tâches répétitives grâce à des scripts. Par exemple, un script bash peut installer automatiquement les dépendances d'un projet, lancer des tests, et déployer une application.

### Accès Direct aux Fonctionnalités Système

Certaines fonctionnalités du système d'exploitation sont uniquement accessibles via le terminal. Par exemple, la gestion avancée des permissions de fichiers, la manipulation de réseaux, ou encore le contrôle des processus en cours d'exécution.

### Utilisation des Outils de Développement

De nombreux outils de développement, comme Git pour le contrôle de version, npm pour la gestion des packages JavaScript, ou Docker pour la conteneurisation, nécessitent l'utilisation du terminal. Savoir utiliser ces outils est crucial pour tout développeur.

### Débogage et Diagnostic

Le terminal permet d'accéder à des outils de débogage et de diagnostic puissants. Par exemple, des commandes comme `top`, `htop`, `ping`, et `traceroute` fournissent des informations détaillées sur les performances du système et le réseau.

---

## Utilisation du Terminal

### Sous Windows
Sur Windows, vous pouvez utiliser l'application "Invite de commandes". Un terminal est également disponible directement dans Visual Studio Code (VSCode), accessible via le raccourci `Ctrl + ù`.

### Sous macOS
Sur macOS, vous pouvez utiliser l'application "Terminal", qui se trouve dans le dossier Applications > Utilitaires. Vous pouvez également accéder au terminal depuis VSCode.

### Sous Linux
Sur Linux, vous pouvez utiliser l'application "Terminal", généralement préinstallée sur la plupart des distributions. Vous pouvez également accéder au terminal depuis VSCode.

---

## Commandes de base

Voici les principales commandes à connaître pour le moment :

- `mkdir nomDossier` : crée un dossier (make a directory)
- `cd cheminDossier` : se déplace dans les dossiers (change directory)
- `touch nomFichier` : crée un fichier
- `ls` : liste les fichiers et dossiers dans le répertoire courant

---

### Comment créer un fichier

#### Sous Windows
Pour créer un fichier sous Windows, utilisez la commande `type` :

1. Ouvrez l'invite de commandes.
2. Naviguez jusqu'au dossier où vous souhaitez créer le fichier en utilisant la commande `cd`.
3. Utilisez la commande `type nul > nomFichier.ext` pour créer un fichier vide. Par exemple :
   ```bash
   type nul > exemple.txt
   ```

#### Sous macOS et Linux
Pour créer un fichier sous macOS ou Linux, utilisez la commande `touch` :

1. Ouvrez le terminal.
2. Naviguez jusqu'au dossier où vous souhaitez créer le fichier en utilisant la commande `cd`.
3. Utilisez la commande `touch nomFichier.ext` pour créer un fichier vide. Par exemple :
   ```bash
   touch exemple.txt
   ```

---

> 📎 **Quelques exemples d’utilisation :**
>
> - Créer un dossier “projet” : `mkdir projet`
> - Se déplacer dans le dossier “projet” : `cd projet`
> - Revenir en arrière d’un dossier : `cd ..`
> - Revenir en arrière de deux dossiers : `cd ../..`
> - Lister le contenu du dossier courant : `ls`
> - Créer un fichier vide nommé `index.html` :
>   - Sous Windows : `type nul > index.html`
>   - Sous macOS/Linux : `touch index.html`

---

### Exercice pratique

Pour mettre en pratique ce que vous avez appris, réalisez les étapes suivantes :

1. **Créez un dossier pour votre projet :**
   - Utilisez la commande `mkdir` pour créer un dossier nommé `mon_projet`.

2. **Naviguez dans ce dossier :**
   - Utilisez la commande `cd` pour entrer dans le dossier `mon_projet`.

3. **Créez un fichier README :**
   - Sous Windows, utilisez la commande `type nul > README.md`.
   - Sous macOS/Linux, utilisez la commande `touch README.md`.

4. **Créez un sous-dossier `src` :**
   - Utilisez la commande `mkdir src`.

5. **Naviguez dans le sous-dossier `src` et créez un fichier `index.html` :**
   - Utilisez la commande `cd src`.
   - Sous Windows, utilisez la commande `type nul > index.html`.
   - Sous macOS/Linux, utilisez la commande `touch index.html`.

6. **Listez le contenu du dossier `src` pour vérifier que le fichier a bien été créé :**
   - Utilisez la commande `ls`.

---

Sans plus attendre, passons à l’écriture de nos premières lignes de code dans le prochain cours sur l’HTML et le CSS !

---

## Liens utiles

- [Documentation officielle du terminal sous Windows](https://docs.microsoft.com/fr-fr/windows-server/administration/windows-commands/windows-commands)
- [Documentation officielle du terminal sous macOS](https://support.apple.com/guide/terminal/welcome/mac)
- [Documentation officielle du terminal sous Linux](https://www.tldp.org/LDP/GNU-Linux-Tools-Summary/html/cat.html)